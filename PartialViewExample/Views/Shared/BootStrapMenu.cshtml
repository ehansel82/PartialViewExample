@model  PartialViewExample.Models.BootstrapMenu

<div class="dropdown">
    <button class="btn btn-primary dropdown-toggle" type="button" data-toggle="dropdown">
        <span class="caret">@Model.HeaderText</span>
    </button>
    <ul class="dropdown-menu">
    </ul>
</div>
